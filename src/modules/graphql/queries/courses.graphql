query GetCourses {
  getCourses {
    id,
    title,
    slug,
    author_info,
    price,
    media {
      id,
      uuid,
      collection_name,
      file_name,
      generated_conversions
    }
  }
}

query GetPopularCourses {
  getPopularCourses {
    id,
    title,
    slug,
    author_info,
    price,
    media {
      id,
      uuid,
      collection_name,
      file_name,
      generated_conversions
    }
  }
}

query GetTopCourses {
  getTopCourses {
    id,
    title,
    slug,
    author_info,
    price,
    media {
      id,
      uuid,
      collection_name,
      file_name,
      generated_conversions
    }
  }
}
